#!/bin/sh
#
# chkconfig: 2345 26 74
# description: 
# processname: pylon

# Source function library.
. /etc/init.d/functions

RETVAL=0
DIR=/usr/local/pylon
TMPDIR=/tmp

start() {
	echo -n $"Starting up pylon server: "
    rm -f $TMPDIR/pylon.log
    $DIR/pylon -d -P $TMPDIR/pylon.pid -L $TMPDIR/pylon.log -F $TMPDIR/pylon_dump
	RETVAL=$?
	#sleep 5;
    #$DIR/pylon_loader.pl
	echo
	return $RETVAL
}

stop() {
	echo -n $"Shutting down pylon server: "
	kill `cat $TMPDIR/pylon.pid`
    rm -f $TMPDIR/pylon.pid
	RETVAL=$?
	echo
	return $RETVAL
}

restart() {
	stop
	start
}

status() {
    $DIR/pylonstatus.pl
}

# See how we were called.
case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  status)
    status
    ;;
  *)
	echo $"Usage: pylon {start|stop|restart|status}"
	exit 1
esac

exit $RETVAL
